export class SearchHistory {
    static async load() {

        
        // console.debug(true)
        // const user = await $idb.getStore('search-history').get(obj => obj === '123');
        // console.debug(false)
        // console.debug(user)

        // console.time('arr')
        // $idb.getStore('search-history').put(Array.from({length: 1000}).map(() => ({id: $.uuidv7().str, name: 'haha'})));
        // console.timeEnd('arr')
    }
}

SearchHistory.load();